{
  "name": "ha.mr",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc && node copy-static.mjs && esbuild node_modules/qrcode/lib/browser.js --bundle --format=iife --global-name=QRCode --outfile=dist/qrcode.js",
    "check": "tsc --noEmit",
    "test": "npm run build && node --test tests/*.test.mjs"
  },
  "dependencies": {
    "qrcode": "^1.5.4"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "@types/qrcode": "^1.5.6",
    "esbuild": "^0.28.2",
    "typescript": "^7.0.2"
  }
}
